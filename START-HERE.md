# Start Here

This folder is already a real git repository. While you were on the phone, `git init` was run and two commits were made — a "commit" is just a save-point (see the roadmap page for the plain-English explanation). You don't need to do anything about that yourself; it's already done.

Run `git log --oneline` any time you want to see the save-points that already exist:

```
49244cd Add Meg AI's full training doc; stub placeholders for Carrie and Linda
c7e08d4 Set up project scaffold: README, roadmap, execution ledger, training-doc template
```

## When you have a real 5 minutes (not a phone-call task)

Open Claude Code (or wherever you're working from) and say something close to:

> "This folder is already a git repo with commits. Create a free GitHub repository called ai-agents-for-ghl for it, connect it, and push what's already here."

From that point on, every future update gets its own commit automatically, and `EXECUTION-LEDGER.md` keeps the same history in plain English for whenever you want to read it instead of the git log.
